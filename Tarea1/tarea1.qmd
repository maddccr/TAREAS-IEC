---
title: "Tarea 1 - IEC 2025"
author: "Madeline Cecere, Florencia Pricolli, Sofia Lerner"
format: html
editor: visual
---

# Práctico 0

## Ejercicio 1

Parte a) Estudio analítico y bosquejo gráfico (ver pdf adjunto)

```{r}
# Parte b)
dist_normal <- function(mu = 0,sigma = 1,delta){  # Función que genera densidades de una normal
  x <- seq(-5, 5, by = delta) # valores de x de -5 a 5
  probs <- (1/(sqrt(2*pi)*sigma)) * exp(-((x - mu)^2)/(2*sigma^2)) 
  return(list(x = x, probs = probs)) # devuelve lista con x y densidades
}

# Parte c) Grafica la normal con diferentes distancias entre los puntos
delta <- c(0.5, 0.1, 0.01)
for(d in delta){
  dt <- dist_normal(0,1,d)
  plot(dt$x, dt$probs, col = "red", xlab = "x", ylab = "Densidad",
       cex = d, main = "Densidad Distribución Normal típica",
       sub = paste0("(con distancia entre valores = ", d, ")"))
}
```

```{r}
# Parte d)

par(mfrow = c(1,3)) # Prepara ventana para 3 gráficos juntos

# Diferentes combinaciones de media y desviación
parametros <- list(
  c(mu = 0, sigma = 1),
  c(mu = 5, sigma = 1),
  c(mu = 5, sigma = 4)
)

for(p in parametros){
  mu <- p["mu"]
  sigma <- p["sigma"]
  dt <- dist_normal(mu, sigma, delta = 0.01)
  plot(dt$x, dt$probs, col = "red", xlab = "x", ylab = "Densidad", type = "l",
       lwd = 2, ylim = c(0, 0.4), main = paste0("Normal (μ=", mu, ",σ=", sigma, ")"))
}
```

## Ejercicio 2

Parte a) Estudio analítico y bosquejo gráfico (ver pdf adjunto)

```{r}
#Para la parte 2 y 3 tomamos mu = 0

dist_cauchy <- function(mu = 0, delta){
  x <- seq(-5, 5, by = delta) #secuencia con distancia delta
  probs <- (1/pi)*(1/(1 + (x - mu)^2)) #densidad // colas pesadas
  return(list(x = x, probs = probs))
} 

#Divido la funcion en dos por la diferencia en escala a la hora de graficar
#Parte 2 y 3
delta <- c(0.5, 0.1, 0.01)
for(d in delta){
  dt <- dist_cauchy(0, d)
  plot(dt$x, dt$probs, col = "red", xlab = "x", ylab = "Densidad",
       cex = d, main = "Densidad Distribución de Cauchy(μ=0)",
       sub = paste0("(con distancia entre valores = ", d, ")"))
}

#GRAFICAR CON TYPE = "l" ?? 

#Parte 4
par(mfrow = c(1,2))
mu <- c(0, 5)
for(m in mu){
  dt <- dist_cauchy(m, 0.01)
  plot(dt$x, dt$probs, col = "red", xlab = "x", ylab = "Densidad", type = "l",
       lwd = 2, main = paste0("Densidad Distribución de\n Cauchy(μ=", m, ")"))
}
```

```{r}
#Distribucion binomial // p en [0,1], x en {0, ..., n}

#descartado // sapply(x, function(y) dist_binom(y, p, n))

plot_binom <- function(n = 50, p){
  x <- 0:n
  probs <- choose(n,x)*(p^x)*(1-p)^(n - x)
  plot(x, probs, type = "h", xlab = "x", ylab = "P(X = x)", col = "blue",
       main = paste0("Cuantia Distribución Binomial (",n, ", ", p,")"))
  points(x, probs, col = "blue", pch = 20)
}

plot_binom(p = 0.2)
plot_binom(p = 0.8)
plot_binom(p = 0.5)
```

## Ejercicio 3

Una empresa le ofrece un puesto de trabajo por un año bajo las siguientes condiciones: En su primer día de trabajo ganará 1 peso. El segundo 2 pesos, el tercer 3 pesos y así sucesivamente.

a - ¿Cuánto dinero ganará al cabo de un año (no bisiesto)?. ¿Cuánto sería su ingreso promedio mensual?.

```{r}
sueldo <- sum(1:365)
sueldo
promedio_mensual <- sueldo/12 
promedio_mensual
```

b - ¿Y luego de dos años?, ¿y el ingreso promedio mensual?

```{r}
sueldo2 <- sueldo + sum(366:730) 
sueldo2
promedio_mensual2 <- sueldo2/24
promedio_mensual2
```

c - Responder 1. y 2. si comienza ganando 100 pesos y tiene el mismo régimen de aumento.

```{r}
sueldo100 <- sum(100:464)
sueldo100
promedio100 <- sueldo100/12
promedio100

sueldo100_2 <- sueldo100 + sum(465:829)
sueldo100_2
promedio100_2 <- sueldo100_2/24
promedio100_2
```

## Ejercicio 4

Un amigo suyo le comunica que perdió las boletas correspondientes a tres compras distintas de los bienes A,B y C. Sin embargo, recuerda que el primer día compró 3 unidades del bien A, 2 del C y gastó 360 pesos. El segundo día compró 4 unidades del bien A, 1 del B y 1 del C y gastó 480 pesos, y el tercer día compró 6 unidades del bien B y 5 del C gastando 450 pesos.

a - ¿Cuánto cuesta cada bien? (resolver analíticamente) - ver foto adjunta

b - ¿Cuánto cuesta cada bien? (resolver usando R)

```{r}
a <- matrix(c(4,3,0,1,0,6,1,2,5 ), nrow = 3)

b <- c(480,360,450)

solucion <- solve(a, b)
solucion
```

# Práctico 1

## Ejercicio 1

Ver pdf adjunto

## Ejercicio 5

*Hallar el error absoluto Ex y relativo Rx y el número de cifras significativas de la aproximación*

-   Ex = \| x - x_gorro \|

-   Rx = Ex / \|x\|

-   Cifras significativas: cantidad de dígitos decimales correctos en la aproximación (desde el primero distinto de cero hasta el error)

```{r}
error_abs <- function(x, x_gorro){
  abs( x - x_gorro)
}

error_rel <- function(x, x_gorro){
  error_abs(x, x_gorro)/abs(x)
}

#a) 
x = 2.71828182 ; x_g = 2.7182 #5 cifras sig
error_abs(x, x_g) ; error_rel(x, x_g) 

#b)
y = 98350 ; y_g = 98000 #2 cifras sig (?)
error_abs(y, y_g) ; error_rel(y, y_g)

#c) 
z= 0.000068 ; z_g = 0.00006 #1 cifra sig
error_abs(z, z_g) ; error_rel(z, z_g)
```

# Tipos de Medias

*Responder qué es la media aritmética, geométrica, armónica (poner fuente de donde se extrajo la información) ver cuál es la más chica y cuál la más grande. Si es posible con un juego de datos a elección calcular las 3 medias*

## Definiciones

La **media aritmética**, también llamada promedio, es un concepto básico de estadística que consiste en la suma de un conjunto de números dividida entre la cantidad de sumandos. Esta medida, que corresponde a una de las llamadas medidas de centralización, se considera el centro de los datos porque equilibra los valores por defecto y por exceso respecto a la media. Es decir, si se suman las desviaciones de los valores respecto a la media se obtiene cero.

$$ 
\bar{x} = \frac{1}{n}\sum_{i=1}^{n} x_i = \frac{x_1 + x_2 + \cdots + x_n}{n}   
$$

La desventaja principal de esta media es que está muy influida por los valores atípicos, ya que un único valor muy alto o muy bajo es capaz de aumentar o disminuir significativamente el valor.

Por otro lado, la **media geométrica** de un conjunto de n números es la raíz enésima del producto de estos. Normalmente se utiliza para comparar conjuntos de valores de diferentes unidades de medida. Esta media no es tan sensible a los datos atípicos como la aritmética, pero su interpretación resulta menos intuitiva. Además, si uno de los valores es cero la media se anula. Solamente sirve para valores positivos.

$$ \bar{x} = \left( \prod_{i=1}^{n} x_i \right)^{\tfrac{1}{n}} $$

Finalmente, la **media armónica** es el inverso de la media aritmética de los inversos de un conjunto de n valores. Es decir, se calcula dividiendo n entre la suma de los valores inversos del conjunto. En este caso, los valores atípicos altos no afectan la media, sin embargo, los bajos sí la influencian. Además, al igual que la geométrica, no está definida en el caso de que exista un valor nulo. Por estas razones, suele ser usada para promediar velocidades o tiempos.

$$ \bar{x} = \frac{n}{\sum_{i=1}^{n} \tfrac{1}{x_i}} $$\
Estas medidas de resumen se conocen como las “medias pitagóricas”. Para todo conjunto de datos se cumple que $\min \;\leq\; M_H \;\leq\; M_G \;\leq\; M_A \;\leq\; \max$ . Esta relación entre ellas es una generalización de la desigualdad de las medias aritmética y geométrica que establece que la media aritmética de un conjunto de números reales positivos es mayor o igual que la media geométrica del mismo conjunto. Y la igualdad se cumple únicamente cuando todos los valores son iguales.

Fuente: Wikipedia, Fundamentos de Estadística (Daniel Peña)

## Cálculo de las tres medias

```{r}
datos <- rexp(100) #simulamos 100 datos con distribucion exponencial

media_aritmetica <- mean(datos)

media_geometrica <- prod(datos)^(1/length(datos))

media_armonica <- length(datos)/sum(1/datos)

data.frame(
  Medias = c("Aritmética", "Geométrica", "Armónica"), 
  Valores = c(media_aritmetica, media_geometrica, media_armonica)
)
```
